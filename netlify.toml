[build]
  publish = "."
  command = "node -e \"const fs=require('fs'); const c=JSON.parse(fs.readFileSync('config.example.json','utf8')); c.googleClientId=process.env.GOOGLE_CLIENT_ID||''; fs.writeFileSync('config.json',JSON.stringify(c,null,2));\""
